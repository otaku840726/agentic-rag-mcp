# ── General Settings ──
LOG_LEVEL=INFO
# GRAPH_PROJECT=my-project                                # Logical isolation key — multiple projects can share the same DB

# ── Qdrant Vector Database ──
QDRANT_URL=https://your-cluster.cloud.qdrant.io
QDRANT_API_KEY=your-qdrant-api-key
QDRANT_COLLECTION=your-collection-name

# ── Provider API Keys (only configure what you use) ──
OPENAI_API_KEY=your-openai-api-key
# OPENROUTER_API_KEY=your-openrouter-api-key
# GEMINI_API_KEY=your-gemini-api-key
# VOYAGE_API_KEY=your-voyage-api-key          # https://dash.voyageai.com → API Keys
# LOCAL_LLM_URL=http://127.0.0.1:1234/v1
# LOCAL_LLM_API_KEY=your-local-api-key

# ── Vertex AI (Google Cloud) ──
# VERTEX_PROJECT_ID=your-project-id
# VERTEX_LOCATION=us-central1
# VERTEX_BASE_URL=                             # Optional override
# VERTEX_API_KEY=                              # Optional: Auto-generated via ADC if empty

# ── Component provider/model overrides (default: openai + gpt-4o-mini) ──
# EMBEDDING_PROVIDER=openai
# EMBEDDING_MODEL=text-embedding-3-small
# EMBEDDING_IDENTIFIER=openai-3-small
# EMBEDDING_MAX_TOKENS=8191
# EMBEDDING_BATCH_SIZE=100
#
# Voyage AI embedding example (voyage-code-3 recommended for code repos):
# EMBEDDING_PROVIDER=voyage
# EMBEDDING_MODEL=voyage-code-3
# EMBEDDING_IDENTIFIER=voyage-code-3
# EMBEDDING_MAX_TOKENS=16000
# EMBEDDING_BATCH_SIZE=128

# Sparse Search settings (qdrant-bm25 | splade | disabled)
# SPARSE_MODE=qdrant-bm25
# BM25_VOCAB_SIZE=30000
# SPLADE_MODEL=prithivida/Splade_PP_en_v1

# Component configuration overrides
# ANALYST_PROVIDER=openai
# ANALYST_MODEL=gpt-4o-mini
# ANALYST_MAX_TOKENS=2000
# ANALYST_TEMPERATURE=0.1

# PLANNER_PROVIDER=openai
# PLANNER_MODEL=gpt-4o-mini
# PLANNER_MAX_TOKENS=4000
# PLANNER_TEMPERATURE=0.1

# SYNTHESIZER_PROVIDER=openai
# SYNTHESIZER_MODEL=gpt-4o-mini
# SYNTHESIZER_MAX_TOKENS=2000
# SYNTHESIZER_TEMPERATURE=0.2

# JUDGE_PROVIDER=openai
# JUDGE_MODEL=gpt-4o-mini
# JUDGE_MAX_TOKENS=1000
# JUDGE_TEMPERATURE=0

# ── Neo4j / AuraDB Graph Database (optional) ──
# NEO4J_ENABLED=true
# NEO4J_URI=bolt://localhost:7687                          # Local Docker
# NEO4J_URI=neo4j+s://xxxxxxxx.databases.neo4j.io         # AuraDB Free
# NEO4J_USERNAME=neo4j
# NEO4J_PASSWORD=your-neo4j-password
# NEO4J_DATABASE=neo4j
#
# Quick setup options:
#   1. AuraDB Free: https://console.neo4j.io → Create Free Instance → copy URI + password
#   2. Docker:      docker run -d -p 7687:7687 -p 7474:7474 -e NEO4J_AUTH=neo4j/testpassword neo4j:5
#   3. Neo4j Desktop: https://neo4j.com/download/

# ── Analyzer Docker images (optional overrides) ──
# ANALYZER_ROSLYN_IMAGE=agentic-rag-roslyn-analyzer:latest
# ANALYZER_ROSLYN_COMMAND="{file_path}"
# ANALYZER_ROSLYN_NUGET_CACHE=~/.nuget/packages:/root/.nuget/packages   # Mount host NuGet cache
# ANALYZER_SPOON_IMAGE=agentic-rag-spoon-analyzer:latest
# ANALYZER_SPOON_COMMAND="{file_path}"
# ANALYZER_SPOON_M2_CACHE=~/.m2/repository:/root/.m2/repository          # Mount host Maven cache

# ── Index filtering (optional, comma-separated) ──
# Server uses a whitelist of known-valuable extensions (.cs, .py, .java, .sql, .md, .yaml, etc.)
# These env vars let you customize without changing code:
# INDEX_EXTRA_EXTENSIONS=.tf,.hcl,.toml,.sh     # Add more indexable extensions
# INDEX_REMOVE_EXTENSIONS=.json,.xml             # Remove extensions from whitelist
# INDEX_EXTRA_EXCLUDE_DIRS=tmp,logs,artifacts    # Add directories to exclude
# INDEX_EXTRA_EXCLUDE_FILES=.generated.cs        # Add filename patterns to exclude (suffix match)
